// Stub gerado pelo enrich-harness — substitua por testes reais.
using Xunit;

namespace DotRMediator.Tests;

public class ResponseHarnessGapTests
{
    [Fact(Skip = "HARNESS_GAP: feature `response` — implementar cobertura. Código: src/response")]
    public void Feature_Response_NeedsUnitCoverage()
    {
        Assert.True(true);
    }
}
