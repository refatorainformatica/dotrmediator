// Stub gerado pelo enrich-harness — substitua por testes reais.
using Xunit;

namespace DotRMediator.Tests;

public class WithoutHarnessGapTests
{
    [Fact(Skip = "HARNESS_GAP: feature `without` — implementar cobertura. Código: src/without")]
    public void Feature_Without_NeedsUnitCoverage()
    {
        Assert.True(true);
    }
}
