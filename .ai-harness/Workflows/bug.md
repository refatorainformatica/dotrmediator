# Workflow — Bug (dotrmediator)

1. Reproduzir (teste falhando preferencialmente)
2. Fix mínimo no código (`src/ ou Apis/Services/Features/`)
3. Tester: regressão `dotnet test DotRMediator.slnx`
4. Reviewer: sem refactor de carona
5. DONE

## DoD

- [ ] Teste de regressão existe (criar se AC-G*)
- [ ] Fix no escopo
